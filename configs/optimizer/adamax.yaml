# @package optimizer
# https://pytorch.org/docs/stable/generated/torch.optim.Adamax.html

_target_: torch.optim.Adamax

params: ???
lr: ${lr} # default: 0.002
betas: [0.9, 0.999]
eps: 1e-08
weight_decay: 0